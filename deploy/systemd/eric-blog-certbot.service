[Unit]
Description=Renew the Eric Blog Let's Encrypt certificate
After=docker.service network-online.target
Wants=network-online.target

[Service]
Type=oneshot
# Certbot writes only the persistent certificate directory. Nginx receives HUP only after a successful renewal check.
ExecStart=/bin/sh -c '/usr/bin/docker run --rm -v /etc/letsencrypt:/etc/letsencrypt -v /var/lib/eric-blog/certbot-www:/var/www/certbot certbot/certbot:v2.11.0 renew --webroot -w /var/www/certbot --quiet && /usr/bin/docker kill -s HUP eric-blog-nginx'
